spring.application.name=api
app.cookie.secure=false
app.baseUrl=http://localhost:4200
# Configuration de la base de donnees pour Spring Data + DDL Flyway
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USER}
spring.datasource.password=${DATABASE_PASSWORD}
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
logging.level.org.flywaydb=DEBUG
# Configuration de Spring Data & Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# Clé secrète pour signer les JWT
application.security.jwt.secret-key=${JWT_KEY}
# Loggers
logging.level.com.cliet_tableaux.api=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=WARN
logging.pattern.console="%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
logging.file.name=logs/app.log
# Cloudinary
cloudinary.cloud-name=dxcrr7aon
cloudinary.api-key=833117657931665
cloudinary.api-secret=${CLOUDINARY_API_SECRET}
# Emails
spring.mail.host=smtp.resend.com
spring.mail.port=465
spring.mail.username=resend
spring.mail.password=${RESEND_API_KEY}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
mail.contact.destinataire=benjamin.liet.dev@gmail.com
mail.contact.expediteur=onboarding@resend.dev
# Clé API pour Stripe
stripe.secret.key=${STRIPE_API_KEY}
stripe.webhook-secret=${STRIPE_WEBHOOK_SECRET}
